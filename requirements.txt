# Core RAG pipeline (LangChain + FAISS)
langchain==0.3.30
langchain-community==0.3.27
langchain-ollama==0.3.10
langchain-text-splitters==0.3.11

# Vector store
# Upper bound kept loose (rather than <1.9) because older faiss-cpu builds
# have no wheel for newer Python ABIs (e.g. hosts running Python 3.13/3.14)
# -- verified faiss-cpu 1.14.x still exposes the same IndexFlatL2/add/search
# API this project uses. See also .python-version, which pins the deployed
# Python version directly so this stays predictable either way.
faiss-cpu>=1.8,<2

# Local Ollama client (used directly to pull/list models)
ollama>=0.5.3,<1.0.0

# Web demo
streamlit>=1.38,<1.40

# Colab-only convenience for exposing the Streamlit demo publicly
pyngrok>=7.0,<8.0

# Live-demo-only backend (LLM_BACKEND=groq). Not used by the default local
# Ollama path -- see README "Live demo" and src/rag.py get_llm/get_embeddings.
# Included here (rather than a separate requirements file) so Streamlit
# Community Cloud's single-requirements.txt auto-install picks them up.
langchain-groq>=0.2,<1.0
langchain-huggingface>=0.1,<1.0
sentence-transformers>=3.0,<6.0
